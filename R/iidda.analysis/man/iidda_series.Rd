% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series.R
\name{iidda_series}
\alias{iidda_series}
\alias{iidda_attach_series}
\alias{iidda_prep_ma}
\alias{iidda_prep_series}
\title{Plot a time series}
\usage{
iidda_series(
  data,
  series_variable = NULL,
  time_variable = NULL,
  trim_series = TrimSeries(zero_lead = FALSE, zero_trail = FALSE),
  handle_missing_values = HandleMissingValues(na_remove = FALSE, na_replace = NULL),
  handle_zero_values = HandleZeroValues(zero_remove = FALSE, zero_replace = NULL),
  time_variable_converter = TimeVariableConverter(),
  title = TitleGuesser(),
  subtitle = TimeRangeDescriber(),
  theme = iidda_theme
)

iidda_attach_series(
  data,
  initial_ggplot_object = ggplot(),
  series_variable = NULL,
  time_variable = NULL
)

iidda_prep_ma(
  data,
  series_variable = NULL,
  time_variable = NULL,
  trim_series = TrimSeries(zero_lead = FALSE, zero_trail = FALSE),
  handle_missing_values = HandleMissingValues(na_remove = FALSE, na_replace = NULL),
  handle_zero_values = HandleZeroValues(zero_remove = FALSE, zero_replace = NULL),
  compute_moving_average = ComputeMovingAverage(ma_window_length = 52),
  time_variable_converter = TimeVariableConverter()
)

iidda_prep_series(
  data,
  series_variable = NULL,
  time_variable = NULL,
  trim_series = TrimSeries(zero_lead = FALSE, zero_trail = FALSE),
  handle_missing_values = HandleMissingValues(na_remove = FALSE, na_replace = NULL),
  handle_zero_values = HandleZeroValues(zero_remove = FALSE, zero_replace = NULL),
  time_variable_converter = TimeVariableConverter()
)
}
\arguments{
\item{data}{A data frame containing the time-series data (typically output from \code{iidda_prep_series()}).}

\item{series_variable}{Name of the series column in \code{data} (e.g., \code{"deaths"}).}

\item{time_variable}{Name of the time column in \code{data} (e.g., \code{"period_end_date"}).}

\item{trim_series}{A \code{TrimSeries(...)} specification controlling removal of leading/trailing zeros.
Default: \code{TrimSeries(zero_lead = FALSE, zero_trail = FALSE)}.}

\item{handle_missing_values}{A \code{HandleMissingValues(...)} specification controlling NA handling.
Default: \code{HandleMissingValues(na_remove = FALSE, na_replace = NULL)}.}

\item{handle_zero_values}{A \code{HandleZeroValues(...)} specification controlling zero handling.
Default: \code{HandleZeroValues(zero_remove = FALSE, zero_replace = NULL)}.}

\item{time_variable_converter}{A \code{TimeVariableConverter(...)} specifying how to parse/convert the
time variable. Default: \code{TimeVariableConverter()}.}

\item{title}{A plot title, or a function that takes \code{data} and returns a
title string.}

\item{subtitle}{A plot subtitle, or a function that takes \code{data} and returns
a subtitle string.}

\item{theme}{A function that returns a \code{ggplot2::theme} object.}

\item{compute_moving_average}{function to compute the moving average of \code{series_variable}}
}
\value{
A \code{ggplot2} plot object containing the time-series line chart with title, subtitle, and theme applied.
}
\description{
Prepare a data set for plotting (trimming, handling missing/zero values, converting the time
variable), build a line chart, and apply title, subtitle, and theme.
}
\section{Functions}{
\itemize{
\item \code{iidda_attach_series()}: Attach a time-series plot to a data frame
containing the plotted data.

\item \code{iidda_prep_ma()}: Prepare a dataset so that it can be used to
produce a time-series plot of a moving average.

\item \code{iidda_prep_series()}: Prepare a dataset so that it can be used to
produce a time-series plot.

}}
\concept{plotting_functions}
